# Corrective verification matrix

| Requirement                           | Offline evidence                                                                                                                                                                                                 | Status / remaining boundary                                                                         |
| ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Trusted bundle reconstruction         | `src/release-bundle.test.ts` reparses exact ZIP bytes and rederives identity, version, catalog, Config, provenance, authority, artifact, and release digests; descriptor/digest/path/link/review tampering fails | Complete offline                                                                                    |
| Reviewed authority lineage            | Bootstrap uses the uniquely derived empty authority snapshot; later versions bind `authorityBefore` to the verified previous identity, release digest, and authority digest                                      | Complete offline; human review still supplies the protected review file                             |
| Publisher lineage freshness           | Before writes, trusted publication compares frozen review lineage to the current latest prior journal row and exact application state; stale, wrong, mismatched, or revoked predecessors fail                    | Complete offline; no live provider call                                                             |
| Approved release set                  | Set digest binds merge SHA plus the exact identity/release/review set; trusted publication compares the environment approval before constructing write adapters                                                  | Complete offline; environment approval is not configured                                            |
| Attempt ordinal authority             | Set digest binds the safe integer ordinal; every bundle must match it and trusted `GITHUB_RUN_NUMBER`; cross-merge retries use a new trusted ordinal without changing immutable content identity                 | Complete offline                                                                                    |
| Cross-merge partial retry             | Exact Phase 1 SQLite + `Phase1D1PublicationAdapter` test publishes coherent A, interrupts coherent B, resumes B from a later merge while retaining the original journal audit fields                             | Complete offline                                                                                    |
| Finalize-before-journal crash         | Exact Phase 1 test finalizes coherent C without journal completion, then reconciles through the normal publisher with no second object PUT                                                                       | Complete offline                                                                                    |
| Missing-journal recovery              | Exact Phase 1 application state plus controlled immutable object state reconstructs a fresh D1 journal row                                                                                                       | Complete offline                                                                                    |
| Unchanged no-op                       | Actual child-process CLI consumes a merge-bound verified baseline, emits an empty release set, and dry-runs with `writes: 0`, `bundles: 0`                                                                       | Complete offline; baseline artifact authority depends on same authenticated workflow-run provenance |
| Durable published baseline            | Read-only exact application state plus bounded complete R2 byte-length/SHA verification precedes `durableStateVerified`                                                                                          | Complete offline adapters; no live provider acceptance                                              |
| Revoked historical row                | Exact immutable revoked state is recorded separately, blocks only selection of that exact source, and does not block unrelated Plugins                                                                           | Complete offline; operator supersession policy remains explicit                                     |
| D1 REST contract                      | Injected HTTP transport proves `{ batch: [{ sql, params }] }`, envelope decoding, result count, and `meta.changes`; exact SQL rollback is proven in local SQLite                                                 | Live Cloudflare REST rollback acceptance not performed                                              |
| Exact Phase 1 schema                  | Hash-pinned `0042_plugin_control_plane.sql` migration `5d834a38ff1fe278e0225ffe16c2c4f03d335cf0a1511fe67e17b2c348c05f2e` runs with foreign keys enabled                                                          | Complete offline                                                                                    |
| Packaged authentication authority     | Strict archive metadata, protected review, release digest, journal envelope, plan expectation, provider registration, D1 stage/finalize, and readback all bind exact `none` or reviewed `github-app` metadata    | Complete offline; provider registration and live Actor delivery remain unverified                   |
| Full MIT package evidence             | GitHub recipe pins full root `LICENSE` and `NOTICE`; inspector checks raw hashes, canonical MIT form, matching notice copyright, and strict nested metadata while legacy fixture bytes remain valid              | Complete offline; evidence is not ownership attestation                                             |
| Config digest identity reuse          | Exact SQLite tests reuse one canonical Config identity across Plugins/versions, retain legacy-ID support, reject semantic conflicts, and retry once against a verified alternate-ID concurrent winner            | Complete offline; no live D1 concurrency acceptance                                                 |
| CI action pins                        | Workflow pins match `compatibility/github-action-pins.json`; pnpm annotated tag uses the independently resolved peeled commit                                                                                    | Complete offline                                                                                    |
| Live publication and Store acceptance | None; no credentials, deployment, provider write, OAuth, installation, invocation, or revocation call occurred                                                                                                   | Pending separate authorization                                                                      |
