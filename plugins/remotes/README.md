# Managed remote candidates

These records preserve fixed provider endpoints and registration modes from primary provider documentation. They are deliberately `staged-unverified`: no live OAuth, catalog capture, eligibility, provider-account, or conformance call was authorized. Their zero catalog digest and empty tool list are explicit sentinels, not reviewed catalogs, and publication validation fails closed.

Gmail is Developer Preview; eligibility and OAuth verification remain unvalidated. Atlassian uses the documented v2 endpoint. Provider facts must be re-reviewed before replacing the staged status.
